import { AppHasRoleDirective } from './app-has-role.directive';

describe('AppHasRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHasRoleDirective();
    expect(directive).toBeTruthy();
  });
});
